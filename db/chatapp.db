CREATE TABLE signals (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  peerId TEXT,
  type TEXT,
  message TEXT,
  timestamp TEXT,
  status TEXT DEFAULT 'unread'  -- standaard is het bericht "ongelezen"
);
